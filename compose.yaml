version: "3.9"

services:
  backend:
    build:
      context: ./Backend

    container_name: cinestack-backend

    ports:
      - "3000:5000"

    env_file:
      - ./Backend/.env

    restart: unless-stopped